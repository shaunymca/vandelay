# Vandelay Environment Variables
# Copy this to .env and fill in your values

# === Model Provider API Keys (set the one you're using) ===
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
# Ollama runs locally — no API key needed

# === Telegram (optional) ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === WhatsApp / Meta (optional) ===
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=

# === Server ===
VANDELAY_HOST=0.0.0.0
VANDELAY_PORT=8000
VANDELAY_SECRET_KEY=change-me-to-a-random-string

# === Database (default: sqlite) ===
# Set to a postgres connection string to use Postgres instead
# DATABASE_URL=postgresql://user:pass@localhost:5432/vandelay
