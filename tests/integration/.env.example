# Integration test credentials
# Copy this file to .env and fill in real values.
#
#   cp .env.example .env
#
# This file is gitignored — never commit real secrets.

# --- Google Drive ---
# Required for Drive tests. Find in Google Cloud Console → project settings.
GOOGLE_CLOUD_QUOTA_PROJECT_ID=your_gcp_project_id

# --- Notion ---
# Create an integration at https://www.notion.so/my-integrations
# Then share your database with the integration.
NOTION_API_KEY=secret_your_integration_token
NOTION_DATABASE_ID=your_database_id_here
# Database must have: "Name" (title) + "Tag" (select) properties
